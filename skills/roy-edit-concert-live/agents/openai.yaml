interface:
  display_name: "Roy Edit Concert Live"
  short_description: "Create approval-gated concert karaoke clips"
  default_prompt: "Use $roy-edit-concert-live to inspect this concert URL, prepare a Bahamut-first lyrics, translation, and line-break approval packet, then stop until I explicitly approve it before making karaoke clips."
